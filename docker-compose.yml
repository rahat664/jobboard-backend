services:
  jobboard-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jobboard-backend
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env.production    # <--- ensures the vars are loaded
    environment:
      NODE_ENV: production
      PORT: "8080"
