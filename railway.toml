# railway.toml — Dockerfile deployment

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "on_failure" # or "always"

[variables]
NODE_ENV = "production"
PORT = "8080"

